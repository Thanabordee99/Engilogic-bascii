# Format: postgresql://<Username>:<Password>@<Hostname>:<Port>/<Database>

# If you are testing locally using Render's database, you MUST use the "External Database URL" 
# (which starts with something like postgresql://... and usually has a different hostname).
# The Internal Database URL (dpg-...) only works when the code is actually running on Render.

DATABASE_URL=postgresql://didactic_dribble_postgres_user:7d84w5CJYjH4oCwPYh2krNNT0skNhBsq@YOUR_EXTERNAL_HOSTNAME:5432/didactic_dribble_postgres